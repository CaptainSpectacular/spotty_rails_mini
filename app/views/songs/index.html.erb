
<div class="links">
  <% songs.each do |song| %>
    <%= link_to song.name, song_path(song), class: 'nav song' %>
    <%= render partial: 'plist_form', locals: { song: song } if current_user %>
  <% end %>
</div>