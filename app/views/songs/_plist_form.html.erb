<%= form_for(:playlist, url: playlist_songs_path, method: :post) do |f| %>
  <%= f.select(:id, options_for_select(options), class: 'playlist-dropdown') %>
  <%= f.label :song_id %>
  <%= f.text_field :song_id, value: song.id, class: 'hidden' %>
  <%= f.submit 'Add to Playlist', class: 'add-to-playlist-button btn' %>
<% end %>


